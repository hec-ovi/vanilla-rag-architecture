# =============================================================================
# Vanilla RAG Architecture - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your machine-specific values
# Do NOT commit .env to git — it contains sensitive and host-specific data
# =============================================================================

# =============================================================================
# HOST PATHS (machine-specific, absolute paths)
# =============================================================================

# Directory for Ollama models (must exist before starting)
OLLAMA_MODELS_DIR=/models/ollama

# Directory for application data (vector DB, uploads, cache)
DATA_DIR=/data

# =============================================================================
# OLLAMA CONFIGURATION
# =============================================================================

# Model to auto-download and use
OLLAMA_MODEL=gpt-oss:20b

# Context window size in tokens (higher = more VRAM)
OLLAMA_CONTEXT_LENGTH=8192

# How long model stays loaded in VRAM after last request (e.g., 5m, 1h, -1 for forever)
OLLAMA_KEEP_ALIVE=5m

# Port for Ollama API (exposed to host)
OLLAMA_PORT=11434

# =============================================================================
# BACKEND CONFIGURATION
# =============================================================================

# Port for FastAPI backend (exposed to host)
BACKEND_PORT=8000

# Logging level: debug, info, warning, error
LOG_LEVEL=info

# Vector database type: faiss or chroma
VECTOR_DB_TYPE=faiss

# Embedding model for vectorization
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Reranker model for semantic reranking
RERANKER_MODEL=cross-encoder/ms-marco-MiniLM-L-6-v2

# Text chunking settings
CHUNK_SIZE=500
CHUNK_OVERLAP=100

# Retrieval settings
TOP_K_RETRIEVE=10
TOP_K_RERANK=3

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================

# Port for Vite frontend (exposed to host)
FRONTEND_PORT=3000
